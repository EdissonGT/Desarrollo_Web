// Objeto Math

let resultado;

resultado = Math.PI; // Mostrar el pi
resultado = Math.round(2.5); // Redondea el valor en parentesis, normal
resultado = Math.ceil(2.1); // Redondea hacia arriba
resultado = Math.floor(2.9); // Redondea hacia abajo
resultado = Math.sqrt(144); // La raiz cuadrada
resultado = Math.abs(-200); // Pasa el numero negativo a positivo
resultado = Math.min( 3, 5, 1, 8, 2, 10); // Retorna el numero minimo del listado
resultado = Math.max( 3, 5, 1, 8, 2, 10); // Retorna el numero maximo del listado
resultado = Math.random(); // Obtener un numero aleatorio, pero no mas de 1
resultado = Math.floor( Math.random() * 30 ); // Usamos 3 metodos, primero multiplicamos el random * 30, luego redondemos hacia abajo




console.log(resultado);
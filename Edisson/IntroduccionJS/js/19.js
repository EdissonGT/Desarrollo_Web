const numero1 = 20;
const numero2 = "20";

console.log( parseInt(numero2)); // parseInt() es una FUNCION que nos ayuda a convertir de String a numero, si es un tipo de INT valido
console.log( numero1.toString() ); // .toString es un METODO, porque va antes la variable, luego el punto.


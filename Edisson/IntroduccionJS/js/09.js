// Booleans

//Declaramos varibales booleanas y una string
const boolean1 = true;
const boolean2 = false;


// usando el typeof, podemos ver que tipo de variable es
console.log(typeof boolean1);
console.log(typeof boolean2);

// Sintaxis que no se usa mucho
const boolean3 = new Boolean(true);
console.log(typeof boolean3);